package ArraysECollections;

public class EqualEHashcode {

	/*
	 * "==" é mais usado em tipos primitivos. "Equals" é usado em todos os tipos de
	 * objetos em forma de função, ele compara o valor do objeto e não o valor de
	 * aloção do obj (memoria);
	 * 
	 * Equal + hascode (conjunto hashset) Equals return true/false (boolean)
	 * Hashcode return int
	 */
}
