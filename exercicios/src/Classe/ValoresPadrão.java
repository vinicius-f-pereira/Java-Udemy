package Classe;

public class ValoresPadrão {

	/*
	 * byte, short, int, long -> 0 float, double -> 0.0 boolean -> false char ->
	 * '\u0000'
	 * 
	 * objetos -> null ex: String s = null;
	 * 
	 * Quando define o valor que é uma constante ele precisa ter uma valor, pois o
	 * valor padrão não funciona. Pode ser na propria constante ou dentro do
	 * construtor.
	 */
}
